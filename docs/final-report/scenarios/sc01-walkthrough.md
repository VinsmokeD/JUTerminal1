# SC-01 NovaMed — Kill-Chain Walkthrough Evidence
<!-- Generated by WS7. To be filled during live demo run. -->

**Status:** TEMPLATE — replace with real terminal transcript during WS10 live walkthrough.

**Target:** 172.20.1.20 (NovaMed Healthcare web server)  
**Profile:** `docker compose --profile sc01 up -d`  
**Scenario:** SC-01 Web App Pentest

---

## Phase 1 — Reconnaissance

```
$ nmap -sV -p 22,80,443,3306,6379 172.20.1.20
[TRANSCRIPT: paste real nmap output here]
```

**Flag triggered:** — (reconnaissance phase, no flag yet)  
**SIEM events observed:** Port scan detected (LOW severity)  
**Phase advancement:** ✅ after nmap + whatweb

---

## Phase 2 — Enumeration

```
$ gobuster dir -u http://172.20.1.20 -w /usr/share/wordlists/dirb/common.txt
[TRANSCRIPT: paste gobuster output here]
```

**Paths discovered:** `/admin`, `/records`, `/.git`, `/.env.bak`  
**SIEM events observed:** Directory scan detected (MEDIUM)  
**Phase advancement:** ✅ after gobuster

---

## Phase 3 — Vulnerability Identification

```
$ curl "http://172.20.1.20/records/?file=../../etc/passwd"
[TRANSCRIPT: paste real LFI output showing root:x:0:0:...]
```

**FLAG-SC01-1 (LFI):** `flag_candidate` nudge fired ✅  
**Submitted via FlagSubmitWidget:** ✅  
**SIEM events:** LFI attempt detected (HIGH)  

---

## Phase 4 — Exploitation

```
$ sqlmap -u "http://172.20.1.20/login" --data="username=admin&password=test" --dbs
[TRANSCRIPT: paste sqlmap output]

$ curl http://172.20.1.20/.env.bak
[TRANSCRIPT: shows JWT_SECRET + DB password]
```

**FLAG-SC01-2 (DB backup):** Captured ✅  
**FLAG-SC01-3 (admin config):** Captured ✅  
**FLAG-SC01-BONUS (.env.bak):** Captured ✅  
**SIEM events:** SQLi attack (CRITICAL), Sensitive file access (HIGH)

---

## Phase 5 — Post-Exploitation

```
$ redis-cli -h 172.20.1.20 -p 6379 info server
[TRANSCRIPT: unauthenticated Redis access]
```

**FLAG-SC01-4 (IDOR patient record):** Captured ✅  
**SIEM events:** Unauthenticated Redis access (CRITICAL)

---

## Final Score

| Metric | Value |
|--------|-------|
| Flags captured | 5/5 |
| Hints used | — |
| Time bonus | — |
| Final score | — |

---

## Network Isolation Verification

```
$ ping 8.8.8.8   # Expected: unreachable (isolation confirmed)
$ curl http://172.20.1.20/   # Expected: 200 OK (internal reach confirmed)
```

**Isolation result:** All-blocked ✅ (verify-network-isolation.sh)
